using System;

public enum TestStatus
{
    New = 0,
    Started = 1,
    Submitted = 2,
    Cancelled = -1
}

public enum ToastType
{
    Information = 0,
    Warning = 1,
    Error = 2
}
public enum Tables
{
    Devices,
    BLE,
    Track,
    Visitors,
    Users
}
public enum Enum_Tasks
{
    None,
    UpdateLocation
}
